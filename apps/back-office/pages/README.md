# Empty Next.js pages directory

This project uses the App Router from the root `app` directory.

The FSD page layer lives in `src/pages`. This empty root `pages` directory keeps Next.js from treating `src/pages` as a Pages Router directory, matching the Feature-Sliced Design Next.js recommendation.
