// 이미 프로젝트가 존재하는 상태에서 추가 생성 시도하는 시나리오.
// 베타 제한 "프로젝트 최대 1개" 와 충돌하므로 서버에서 막혀야 하고
// UI 에 적절한 에러 토스트가 떠야 한다.
//
// TODO: 사전 setup 이 까다로워 일단 골격만 둠.
// - 옵션 A: API/server-action 직접 호출해서 baseline 프로젝트 생성
// - 옵션 B: 첫 번째 test 에서 정상 생성 → 두 번째 test 에서 추가 시도
//   (test 간 격리가 깨져 추천 안 함)
// - 옵션 C: DB seed fixture (가장 깔끔, 따로 인프라 필요)
import { test } from './_support/fixtures';

test.describe('프로젝트 생성 - 다중 프로젝트 시나리오', () => {
  test.fixme('이미 프로젝트가 있는 상태에서 추가 생성 시 에러 토스트', async () => {
    // 1. 사전조건: 1개 프로젝트가 이미 존재
    // 2. 모달 열고 Golden Path 와 동일하게 입력
    // 3. Step3 생성하기 클릭
    // 4. expect: 에러 토스트 "생성 실패: ..." 노출
    // 5. expect: 모달이 닫히지 않음 (재시도 가능 상태)
  });
});
